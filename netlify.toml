[build]
  command = "npm install && npm run build"
  publish = "dist"

# Since this is a React app (SPA), we need to redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
